# Changelog

이 프로젝트의 모든 주요 변경 사항을 기록한다. 형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)를 따른다.

## [1.0.144] - 2026-08-17

### Added
- **여행 위시리스트 정보 보강.** 구글 지도 장소를 담으면 서버가 **Places API 로 한 번에** 대표사진·별점(리뷰 수)·카테고리(호텔/리조트 등)·나라+도시(예: "베트남 하노이")·좌표를 받아 저장한다(저장 시 1회만 호출, 같은 링크는 서버 캐시로 재과금 없음, 사진도 서버가 캐시 → 이후 재호출 없음).
- 목록 카드에 **대표사진 썸네일**, 카테고리·지역 칩, **⭐별점(리뷰 수)**, 메모 20자 미리보기를 표시(주소는 목록에서 제외).
- **카테고리·지역 필터**를 가볼 곳과 동일하게 **[필터] 버튼 + 바텀시트** 구조로(칩이 많아도 화면을 밀지 않음). 항목을 **길게 눌러** 메모·카테고리·장소명 편집.

### Fixed
- 사진은 서버 base 가 바뀌어도 안 깨지게 **ref 만 저장**하고 표시 시 URL 조립.
- 필터로 고른 값이 편집·삭제로 사라지면 **필터 자동 해제**(빈 화면 갇힘 방지) + 결과 0건 안내.
- 여러 링크를 연속 공유할 때 앞 결과가 뒤 항목에 섞이지 않도록 **공유 식별 검증**.

## [1.0.143] - 2026-08-13

### Added
- **여행 위시리스트(신규 카드).** 구글 지도에서 장소를 **'공유'하면 여행 위시리스트에 담긴다**(국내·해외 모두). 리스트의 '🧳 여행 위시리스트' 카드로 들어가고, 항목을 **탭하면 구글 지도로 열리며**, **길게 누르면** 주소·메모·편집·다녀옴·삭제 메뉴가 나온다. 메모에 예약·영업시간·팁 등 긴 내용을 붙여넣을 수 있다. (기존 '가볼 곳'(네이버 평점·필터)은 그대로 유지 — 별도 카드)

## [1.0.142] - 2026-08-13

### Fixed
- **이미지를 크게 본 뒤 폰 이전 버튼을 누르면 홈으로 튀던 문제 수정.** 이제 이전 버튼이 확대 보기를 닫고 원래 목록으로 돌아간다(가족 쿠폰함·재미진 곳·사진첩 확대 보기 공통).

## [1.0.141] - 2026-08-13

### Changed
- 가족 쿠폰함: **사용완료한 쿠폰은 맨 뒤로** 정렬(안 쓴 쿠폰이 앞).
- 쿠폰을 **탭하면 쿠폰 이미지(또는 링크/문자)가 바로** 열린다.
- **편집·삭제는 쿠폰을 길게 눌러** 나오는 메뉴에서 한다(유효기간·메모도 이 메뉴에 함께 표시).

## [1.0.140] - 2026-08-13

### Added
- **쿠폰 유효기간(달력 선택) + 만료 관리.** 쿠폰에 유효기간을 D-Day 항목처럼 **달력**으로 넣을 수 있고, **만료일이 가까운 순으로 정렬**된다. 카드에 "D-3" 같은 배지 표시. **만료일 다음날 자동 삭제**(서버가 처리, 앱 미실행이어도 동작).
- **쿠폰 상세 화면(카드 탭).** 이미지 크게 보기·링크 열기·유효기간과 함께, **메모**(혜택 조건·사용 가능 매장 등 긴 내용을 붙여넣고 길게 눌러 복사 가능)를 넣을 수 있다. 상세에서 사용/편집/삭제도 가능.

### Changed
- **가족 쿠폰함 카드를 한 줄에 3개**로 작게 배치.

## [1.0.139] - 2026-08-13

### Changed
- **가족 쿠폰함: 사용완료 표시에 사용한 사람 이름 추가.** 회색 처리된 쿠폰에 **"사용완료 - 준호"**처럼 누가 썼는지 함께 보여준다.

## [1.0.138] - 2026-08-13

### Added
- **가족 쿠폰함 쿠폰에 짧은 설명(제목) 달기.** 쿠폰을 공유한 사람(또는 부모)이 카드 우상단 **연필 아이콘**으로 간단한 설명을 넣을 수 있다(예: "스타벅스 아메리카노 쿠폰"). 이미지 쿠폰은 사진 아래에 설명이 표시돼 무슨 쿠폰인지 한눈에 알 수 있다.

## [1.0.137] - 2026-08-13

### Changed
- **가족 쿠폰함 버튼 표시를 명확하게.** 아직 안 쓴 쿠폰은 **"사용"**, 사용완료한 쿠폰은 **"사용완료"**로 표시(이전엔 안 쓴 쿠폰에도 "사용완료"라고 적혀 헷갈렸음). 사용완료를 누른 본인은 '사용완료'를 다시 눌러 취소.

## [1.0.136] - 2026-08-13

### Changed
- **홈 '오늘, 그날의 추억' 사진을 보고 뒤로 가면 사진첩이 그 사진의 년·월 위치로 스크롤**된 채 열린다(그 시절 사진을 이어 보기 편하게). 뷰어를 여는 순간 그리드를 미리 그 달의 헤더로 이동시켜 둔다.

## [1.0.135] - 2026-08-13

### Fixed
- **홈 '오늘, 그날의 추억' 사진을 두 번째부터 눌렀을 때 상세가 안 열리고 사진첩 목록만 뜨던 문제** 수정. 공유 플래그 방식 대신 **내비게이션 인자로 그 사진 id를 직접 전달**하도록 바꿔, 매번 눌러도 그 사진 상세(뷰어)가 안정적으로 열리게 했다.

## [1.0.134] - 2026-08-13

### Added
- **가족 쿠폰함.** 리스트에서 D-Day 옆 **🎟️ 가족 쿠폰함** 카드로 들어간다. 재미진 곳/내 재미진 곳에서 항목을 **롱클릭 → '가족 쿠폰함으로 이동'**으로 쿠폰(이미지·링크·문자)을 모은다. 필요한 사람이 탭해서 열고(이미지 보기·링크 열기·문자 복사), 다 쓰면 **'사용완료'**를 눌러 회색 처리(다시 못 씀). 사용완료를 누른 본인은 다시 눌러 **취소** 가능. 부모(선일·은선)는 쿠폰을 **삭제**할 수 있다.

### Changed
- 리스트의 **D-Day 카드를 가족 사진 카드 크기로 축소**하고 그 오른쪽에 가족 쿠폰함 카드를 배치.

## [1.0.133] - 2026-08-13

### Fixed
- **홈 '오늘, 그날의 추억' 사진을 누르면 그 사진 상세(뷰어)가 바로 열리도록** 수정(예전엔 사진첩 목록으로만 이동). 사진첩이 열리면서 해당 사진 뷰어가 자동으로 뜬다. (추억에는 **가족 사진첩만** 표시되며 '내 사진첩'(개인) 사진은 포함되지 않음)

## [1.0.132] - 2026-08-13

### Fixed
- **관리 기능 화면이 아래로 잘리던 문제.** 화면에 세로 스크롤을 추가해, '용돈 미션 챌린지' 카드의 준호 항목과 준영 이름 하단까지 모두 보이도록 고쳤다(내용이 길어도 스크롤로 전부 확인 가능).

## [1.0.131] - 2026-08-13

### Added
- **용돈 미션 챌린지(앱 업데이트 챌린지).** 부모가 **관리 기능 → 🎮 용돈 미션 챌린지**에서 아이 이름 옆 '진행'을 누르면, 아이에게 **"🎮 용돈 미션 챌린지! 앱 업데이트 하면 즉시 용돈 2,000원!"** 알림이 간다. 아이가 알림을 누르면 업데이트가 시작되고, **최신 버전으로 업데이트가 완료되면** 자기 용돈에 **"M월 D일 - 앱 업데이트 챌린지 수행완료!" + 2,000원**이 자동으로 추가되고 용돈 화면으로 이동하며 **"업데이트 챌린지 성공! 🎉"** 토스트가 뜬다. (실제로 버전이 올라야만 지급 · 수락 14일 내 미완료 시 자동 만료 · 중복 지급 방지)

## [1.0.130] - 2026-08-13

### Fixed
- **재미진 곳 공유 알림을 눌러도 해당 항목이 안 열리고 목록만 뜨던 문제.** 받은 공유 알림은 보통 앱이 꺼진 상태에서 누르는데, 그 순간엔 로그인·네트워크가 아직 준비되지 않아 항목 조회가 잠깐 실패했고 앱이 바로 포기했다. 이제 준비될 때까지 잠시 재시도해서 **알림을 누르면 공유된 항목(사진·영상·링크)이 자동으로 열린다.**

## [1.0.129] - 2026-08-13

### Changed
- **사진첩 스크롤 막대 색을 원래의 반투명으로 되돌림**(레일·흰 테두리 제거). 위치 정확도(1.0.128)는 유지.

### Fixed
- 코드리뷰 반영: **EXIF·촬영정보가 없는 사진도 원본 파일명(예: `20160904_123145`)에서 촬영시각을 읽어** 올바른 연·월로 정렬(앱·웹·서버 규칙 통일). 불가능한 날짜(2월 31일 등)는 무시.
- 코드리뷰 반영: 사진첩 스크롤 막대가 **줄마다 살짝 끊기던 움직임을 부드럽게**(같은 줄 항목수로 줄 내부 진행 보정).

## [1.0.128] - 2026-08-13

### Fixed
- **사진첩 스크롤 막대가 몇 번 만에 맨 아래로 내려가 안 움직이던 문제(정확도 개선).** 예전엔 '첫 보이는 항목 번호 ÷ 전체 개수'라는 거친 비율로 막대 위치를 잡아, 사진이 점진적으로 로드되고(개수가 계속 바뀜) 여러 열·월 헤더가 섞이면 위치가 어긋났다. 이제 **그리드의 실제 스크롤 정보(현재 총 항목·화면에 보이는 항목·항목 내부 스크롤량)로 계산**해, 막대가 **콘텐츠 실제 맨 아래에서 정확히 끝까지** 가고 스크롤 위치를 정밀히 따라간다. **막대 크기도 사진이 많을수록 작아지게**(남은 양이 보이게) 바꿨다.

## [1.0.127] - 2026-08-13

### Fixed
- **사진첩 빠른 스크롤 막대가 안 보이던 문제.** 아래로 스크롤하다 막대가 사라져 보이던 것을 고쳤다: **항상 보이는 옅은 레일**을 두고, 현재 위치 막대(썸)를 **불투명 파랑 + 흰 테두리**로 바꿔 밝은 사진 위에서도 선명하게 보이게 했으며, 막대 위치를 트랙 안으로 방어적으로 보정했다. (드래그 상태가 고착되지 않도록 정리도 추가)

## [1.0.126] - 2026-08-13

### Changed
- **이미지 캐시 확대(썸네일 재다운로드 감소).** Coil 디스크 캐시를 기본(여유공간 2%, 공유·가변) 대신 **512MB로 고정**해, 사진첩·재미진 곳 등 이미지가 많아도 한 번 본 썸네일이 오래 유지되도록 했다. 먼 과거로 스크롤할 때 다시 받아오는 일이 크게 줄어든다. (기존 캐시 폴더 재사용, 서버 immutable 30일 헤더와 함께 동작)

## [1.0.125] - 2026-08-13

### Changed
- **사진첩 빠른 스크롤 방식 개선.** 오른쪽 빠른 스크롤 막대를 이제 **현재 위치 막대(썸)를 잡고 끌어야** 이동한다. 예전엔 띠의 아무 곳이나 누르면 그 위치(먼 연도)로 바로 튀어 불편했는데, 막대에서 먼 곳을 누르면 점프하지 않고 뒤의 사진 탭으로 넘어간다. 막대도 조금 더 잘 보이게 했다.

## [1.0.124] - 2026-08-13

### Added
- **사진 업로드 시 원본 파일명 저장.** 사진첩에 사진을 올릴 때 원본 파일명을 함께 기록한다(백업 매니페스트·EXIF 없는 사진의 촬영시각 보조용). 앱 사진은 EXIF로 시각이 이미 정확하므로 화면 동작 변화는 없음.
- (서버·웹) 사진첩 **날짜별 백업 export**(촬영시각 기준 `YYYYMMDD_HHMMSS.jpg` + 연/월 폴더, 하드링크라 추가 디스크 없음, 매일 자동 생성) + **EXIF 없는 웹 업로드는 원본 파일명에서 촬영시각 추출**.

## [1.0.123] - 2026-08-13

### Changed
- **가족 사진첩·내 사진첩을 헤르메스 서버(Postgres)로 이관.** 사진이 늘면서 Firestore 무료 읽기 한도를 초과해 리스트가 안 보이던 문제를 없앴다. 사진첩을 페이지 단위로 나눠 불러와(한 번에 전부 읽지 않음) 읽기량을 크게 줄였다. 좋아요·댓글·회전·이동/복사·삭제·"그날의 추억"·개수 표시 모두 새 서버로 동작한다. (사진 파일과 나머지 기능은 그대로)
- **용돈 정산 업데이트 안내를 토스트로.** 버전이 많이 낮을 때 용돈 정산 화면에서 자동으로 뜨던 설치 창을, 화면 상단에 3초간 표시되는 안내 토스트로 바꿨다(덜 방해되게).
- **배터리 사용 개선.** 집 WiFi 재실 확인 보고 주기를 정확 알람 5분 → 여유 알람 15분(Doze 친화)으로 완화해 배터리 소모를 줄였다.

### Added
- **집 WiFi 기반 재실/외출 확인(위치 권한 불필요).** 가족 모두, 폰이 집 WiFi(Home2G/Home5G)에 붙어 있으면 '재실', 아니면 '외출'로 표시된다. 위치 공유를 원치 않아도 재실 여부를 알 수 있다.
- **AI 맛집 찾기 위치 권한 안내.** '추천받기'를 누를 때 위치 권한이 꺼져 있으면 "위치 권한을 켜면 더 정확히 추천해요" 토스트로 안내한다.

### Fixed
- 코드리뷰 반영: **내 사진첩이 다른 가족에게 노출되지 않도록 서버에서 소유자를 강제**(로그인한 본인 것만 조회).
- 코드리뷰 반영: 사진 댓글 삭제·수정 시 **그 사이 달린 다른 댓글이 사라지던 문제 방지**(서버에서 해당 댓글만 조작).
- 코드리뷰 반영: 사진 이동/복사 시 촬영 시각만 같은 **다른 사진을 중복으로 오인해 삭제하던 문제 수정**(같은 파일일 때만 중복 처리).
- 코드리뷰 반영: 사진첩 새로고침 중 네트워크 오류로 **목록이 일부만 남던 문제 방지**(정상 완료 시에만 갱신).

## [1.0.122] - 2026-08-12

### Changed
- **앱 내부 이동이 전반적으로 빨라짐.** 홈·달력·리스트·용돈 등 자주 쓰는 데이터를 앱 실행 직후 미리 불러와 첫 진입에도 스피너 없이 바로 표시하고, 화면을 오갈 때 데이터 구독이 끊겼다 다시 붙는 지연을 줄였다(유지 시간 5초→60초). 커스텀 리스트도 캐시해 재방문이 즉시 뜬다.
- **메뉴 전환 페이드를 절반(700→350ms)으로** 단축.
- **하단바 아이콘 선택 표시를 탭하는 즉시** 반영(예전엔 화면 전환이 끝난 뒤에야 선택 표시가 떴다).

## [1.0.121] - 2026-08-12

### Changed
- **가족 달력 좌우 스와이프가 훨씬 부드러워짐.** 월 전환을 손가락을 따라 실시간으로 미끄러지는 방식(HorizontalPager)으로 바꾸고, 각 달을 렌더 레이어로 캐시해 드래그 중 끊김을 없앴다. 짧게 밀어도 넘어가고, 전환 속도도 빨라졌다.
- 음력·공휴일을 로컬에 캐시(음력 계산 결과 재사용, 공휴일 디스크 캐시)해 달력 렌더와 로딩을 가속.

### Added
- **용돈 정산 화면 업데이트 안내.** 앱 버전이 최신보다 많이(끝자리 30 이상) 낮은 경우, 용돈 정산 화면에 들어가면 업데이트 설치 창을 자동으로 띄운다.

## [1.0.120] - 2026-08-12

### Added
- **새 가족 공지 푸시 알림.** 가족 공지사항에 새 공지를 올리면 가족(작성자 제외) 전원에게 **"📢 새 가족 공지"** 푸시 알림이 간다. (그동안 공지는 앱을 열어야만 홈 화면 강조로 보였고, 푸시 알림은 없었다.)

### Changed
- **맛집 바로가기 위젯 아이콘 확대.** 1x1 위젯의 🍽 아이콘(24→44dp)과 "맛집" 글자(12→18dp)를 크게 키움.

## [1.0.119] - 2026-08-12

### Changed
- **맛집 위젯을 1x1 '맛집 바로가기'로 교체.** 기존 추천 위젯(근처 맛집 자동 검색·회전)은 구글 지도 API를 계속 호출해 비용이 발생했다. 데이터 없이 **탭하면 앱의 맛집 화면으로 바로 가는 1x1 위젯**으로 바꿔 위젯에서의 구글 호출을 완전히 제거했다. (홈에서 기존 위젯을 삭제하고 다시 추가해야 새 모양으로 보임)
- **맛집 추천 비용 최적화(서버).** '추천받기'가 구글 Text Search를 1회만 호출(기존 최대 2회)하고, 주변에 평점 4.3+ 맛집이 없으면 낮은 평점을 억지로 끌어오지 않고 빈 결과로 둔다. → 가족 4명이 하루 5번씩 눌러도 무료 한도 안에서 동작.

### Removed
- 구 맛집 위젯 스케줄러·워커·미사용 드로어블 삭제(내부 정리, 죽은 코드 제거).

## [1.0.118] - 2026-08-11

### Fixed
- **유튜브 쇼츠·임베드 제한 영상 공유 시 제목이 "- YouTube"/"링크"로 나오던 문제 수정.** 재미진 곳에 유튜브(쇼츠 포함)를 공유하면 실제 영상 제목이 표시된다. (앱: 공유 텍스트 제목 우선 처리 / 서버: oEmbed 실패 시 영상 페이지에서 실제 제목 추출)

## [1.0.117] - 2026-08-11

### Fixed
- **주차 위치 위젯이 30분 자동 갱신인데 "주차/확인"이 뜨던 불합리 수정.** 조회값이 35분(신선도)을 넘으면 "확인"으로 바뀌었는데, 자동 갱신(30분)이 절전(Doze)으로 지연되면 "확인"이 떴다. 주차 자리는 주차 중 바뀌지 않으므로 **마지막 조회한 층을 계속 표시**하도록 변경(차가 나가면 조회 시 "출차", 30분 자동 갱신·탭으로 최신화). 최초(조회 이력 없음)에만 "주차/확인"(탭 유도).

## [1.0.116] - 2026-08-11

### Added
- **가족 사진첩 + 내 사진첩 분리.** 리스트 화면에 반반 카드 2개 — **가족 사진첩**(공용)과 **내 사진첩**(본인만 조회·편집). 사진 추가·회전·좋아요·댓글·태그·검색 모두 양쪽에서 동작.
- **월 대표 타이틀 강조 + 태그.** 월 헤더에 주황 강조 바 + 큰 월 표기. 헤더를 탭/롱클릭하면 **월 태그**(예: "오사카 가족여행, 퍼스트가든") 편집 — 가족 사진첩 태그는 가족 모두, 내 사진첩 태그는 본인만.
- **태그 검색.** 상단 검색 아이콘 → 태그·년월로 검색 → 결과를 누르면 그 년월로 스크롤 이동.
- **사진 이동 / 복사.** 롱클릭 메뉴에서 가족↔내 사진첩으로 이동·복사. 대상에 이미 있으면 복사 안 하고 안내(이동은 원본 정리). 이동은 삭제 권한이 있을 때만.
- **여러 장 선택 → 일괄 다운로드·공유·삭제.** 상단 선택 모드, 전체 선택/해제, 하단 액션바. 삭제는 권한 있는 사진만(권한 없는 건 제외하고 토스트 안내).
- **뷰어 하단 다운로드·공유·삭제**(아이콘+텍스트). 타일 하단에 **댓글 수(💬)** 표시(있을 때).
- 홈 화면 **"📸 오늘, 그날의 추억"**(N년 전 오늘).
- **장보기: 체크한 항목 3일 후 자동삭제** + 리스트 상단 안내 문구. *(삭제 스윕은 notify 서버, 별도 배포)*

### Changed
- **날씨를 헤르메스 서버 캐시에서 조회.** 앱마다 Open-Meteo를 직접 부르던 것을, 서버가 백석동 기준 매시간 1회 조회·캐시하고 앱은 결과만 읽도록 변경(장애 시 직접 조회 폴백). *(notify 서버, 별도 배포)*
- **일정 보드 날씨 로드 시 높이 튐 제거**(처음부터 아이콘 크기 포함 자리 확보).

### Fixed
- **댓글 저장이 동시에 눌린 좋아요·회전·별점을 덮어쓰던 문제**(전체 문서 set → 필드 단위 갱신).
- **연사 사진이 업로드 중복으로 잘못 제외되던 문제**(배치 내 중복은 촬영시각+파일크기로 판정).
- **이동/복사 중복검사가 화면에서 안 본 앨범엔 동작 안 하던 문제**(대상 앨범을 실시간 조회).
- 촬영 시각(EXIF)·방향(서버 썸네일 이중회전)·유튜브 공유 제목(oEmbed) 관련 수정 포함(1.0.107~115 누적).

## [1.0.106] - 2026-08-11

### Added
- **가족 사진첩(Album).** 리스트 화면에 '가족 사진첩' 카드 추가 — 사진을 올려 **촬영일 기준 월별 타임라인**(3열 그리드)으로 함께 본다.
  - **앱 자체 사진 그리드로 선택**: 시스템 선택기가 기기(예: 삼성 안드12)마다 파일 브라우저로 열려 매번 맨 위로 초기화되던 문제를 해결하기 위해, 앱 안에서 **카메라(DCIM/Camera) 사진을 최근순 그리드**로 고른다. 여러 장 선택(순번 표시), **오른쪽 빠른 스크롤 + 년월 버블**, 스크롤 위치 유지.
  - **중복 자동 제외**: 이미 올린 사진(같은 촬영시각)은 업로드하지 않고 건너뛴다("N장 추가 · 중복 M장 제외").
  - **사진 뷰어**: 좌우 스와이프로 이전/다음, **더블탭 확대**(썸네일 확대, 원본 미다운로드), **❤️ 좋아요·댓글**. 썸네일 하단에 촬영 날짜·시간·좋아요 수 표시.
  - **롱클릭 메뉴**: 왼쪽으로 회전 / 다운로드(공용 Download) / 공유 / 삭제. **삭제는 확인 후**, 권한은 **올린 사람 또는 선일(관리자)만**(댓글 삭제도 동일).
  - **회전**: 원본 재업로드 없이 표시 각도만 저장해 즉시 반영(화질 보존), 공유·다운로드 시 적용.
  - 홈 화면 **"📸 오늘, 그날의 추억"**: N년 전 오늘 찍은 사진을 가로로 표시.
  - 촬영 시각은 사진 EXIF(androidx ExifInterface)에서 읽어 월 구분·정렬에 사용.

### Fixed
- **재미진 곳/내 재미진 곳 유튜브 공유 제목이 "- YouTube"(또는 "링크")로 찍히던 문제.** 유튜브는 공유 텍스트의 실제 제목을 우선 사용하고, 서버가 유튜브 **oEmbed**로 실제 영상 제목·썸네일을 받아오도록 수정 — URL만 공유해도 제목이 정상. *(notify 서버 변경, 별도 배포 완료)*
- **사진이 옆으로/상하 뒤집혀 보이던 방향 문제.** 서버 썸네일 생성기(Jimp)가 EXIF 방향을 픽셀에 반영하고도 방향 태그를 출력에 다시 써넣어 뷰어가 한 번 더 회전(이중 회전)하던 것을, 썸네일에서 방향 태그를 제거하도록 수정. *(notify 서버 변경, 별도 배포 완료)*

## [1.0.82] - 2026-08-11

### Changed
- **주차 위치 위젯: 06~23시 30분마다 자동 갱신 추가.** 홈 페이지를 넘겨 위젯을 볼 때 대체로 최신 상태(탭 즉시조회는 유지). 안드로이드가 '위젯이 보이는 순간'을 알려주지 않아 주기 갱신으로 구현(자동 갱신은 무음). RTLS(아파트 서버) 호출이라 Google API 비용과 무관. 위젯 제거 시 자동 갱신도 중지(onDisabled).

## [1.0.81] - 2026-08-10

### Changed
- **맛집 위젯: 주행 중 Google API 호출 방지.** 차로 이동하며 지역 경계를 넘을 때마다 호출되던 문제를, **멈춰서 '도착'했을 때만** 재검색하도록 변경. 조건(모두 충족): ① 직전 10분간 400m 미만 이동(정지) ② 마지막 검색 위치에서 **5km 이상** 벗어남 ③ **20분 쿨다운** 경과. → 주행 중에는 호출하지 않고, 새 지역에 도착해 머물 때 1회만.

## [1.0.79] - 2026-08-10

### Changed
- **맛집 위젯 Google Places API 호출 대폭 절감.** 시간 기반(1시간마다) 재검색을 제거하고 **위치(시군구)가 바뀔 때만** 재검색하도록 변경(무캐시 최초 제외). 위젯 요청도 20→10곳.
  - 배경: 위젯이 시간당 재검색 + 호출당 searchText 최대 3페이지(최상위 요금 티어)로 무료 쿼터를 과소비. 서버 `/recommend`도 페이지 3→2·후보 30→16으로 호출당 Google 호출 축소(별도 배포).

## [1.0.78] - 2026-08-09

### Fixed
- **앱이 백그라운드에 계속 살아있을 때 홈·관리 화면 정보가 갱신되지 않던 문제.** 갱신이 route(화면) 변경 시에만 걸려 있어, 같은 화면인 채로 포그라운드 복귀하면 갱신이 안 됨. 각 화면 라이프사이클 **ON_RESUME**(진입/복귀)마다 최소 1회 갱신하도록 변경.
  - 홈: `refreshUpdate()`(업데이트 아이콘 최신화).
  - 관리: `refreshPresence()` + `refreshUpdate()`(가족 접속 시간·버전, 업데이트 상태 최신화).
  - AppNav 의 `route==HOME` 갱신 로직은 제거(HomeScreen ON_RESUME 가 대체, 중복 호출 방지).

## [1.0.77] - 2026-08-09

### Changed
- **주차 위치 위젯 디자인 개선(1.0.71~77).** 라벨("1 :"/"2 :") 제거하고 **층 값만 상하로 크게**(34sp Bold) — 위=모닝(진한 아이보리 `#D9C06A`), 아래=그랜저(흰색). **남색 전체 배경**(`widget_parking_solid`). 데이터 없으면 "출차". 20분 경과 대기 상태는 큰 "주차 / 확인" 두 줄. 두 층 사이 간격은 가중치 스페이서(`parking_gap`)로 좁힘.

### Notes
- 위젯 레이아웃 구조가 바뀌었으므로(간격용 스페이서 뷰 추가 등), **기존에 배치한 주차 위젯은 삭제 후 다시 추가**해야 새 디자인·간격이 적용됨(안드로이드가 배치된 위젯의 옛 레이아웃을 캐시).

## [1.0.70] - 2026-08-09

### Added
- **주차 위치 1x1 홈 위젯.** 위젯을 탭하면 아파트 RTLS(`122.199.183.213`, 104동 3406호)를 1회 조회해 모닝(시리얼 A006524)·그랜저(A033882)의 주차 층을 표시. 응답 HTML의 `tagLocationAction` 시리얼로 각 차량을 매칭해 `carFloorNameArea` 층을 파싱(순서/누락에 안전).
  - **표시**: 조회값 `1 : B2` / `2 : B4`(18sp) · 데이터 없음 `1 : 출`(자리 절약) · 20분 경과/대기 시 1/2 구분 없이 큰 `클릭`(26sp) 한 줄 · 조회 중 `1 : …` · 실패 `조회 실패`/`다시 탭`.
  - **과도 조회 방지**: 자동 폴링 없음(탭할 때만 네트워크 1회). 조회 후 20분간 값 유지, 정확히 20분 뒤 1회 되돌림 알람으로 `클릭` 상태 복귀(재조회 아님 → 루프 없음).
  - 네트워크 보안 설정 cleartext 허용에 RTLS IP 추가.

## [1.0.67] - 2026-08-09

### Changed
- **빠른 연락 요청: 받는 사람이 폰을 사용 중이어도 전체화면으로 표시.** 기존엔 전체화면 인텐트(FSI)라 사용 중일 때 안드로이드가 상단 배너로 낮췄음(OS 설계). `SYSTEM_ALERT_WINDOW`(다른 앱 위에 표시) 권한을 추가해 백그라운드 액티비티 실행 제한(Android 10+)의 예외로 만들어, `showEmergency`의 `startActivity(EmergencyActivity)`가 사용 중에도 성공. 권한 미허용 시 기존 FSI(잠금/화면 꺼짐 전체화면)로 폴백.
- MainActivity 시작 권한 체인에 오버레이 권한 요청 단계(`ACTION_MANAGE_OVERLAY_PERMISSION`) 추가 — 앱 첫 실행/업데이트 후 자동 요청. **받는 사람 폰에 이 권한이 켜져 있어야 사용 중 전체화면 동작**(보내는 사람은 불필요).

## [1.0.66] - 2026-08-08

### Added
- **재미진 곳/내 재미진 곳 → 가족에게 공유**: 항목 롱클릭 → "가족에게 공유" → 나 제외 가족 복수 선택 → 알림 전송. 받는 사람이 **알림 탭 → 그 항목 자동 열림**(영상/이미지/링크). 비공개(내 재미진 곳) 항목 공유 시 **전체 목록 노출 없이 그 항목만** 표시(id 단건 조회).

### Changed
- **접속 현황**: 본인 행은 '업데이트 요청'이 아니라 **'업데이트'**(홈 아이콘과 동일 동작=다운로드·설치). 최신 판정에 배포(version.json) 버전 포함.
- **업데이트 요청 알림 탭 → 앱에서 업데이트 창 자동 표시**(기존엔 앱만 열림). 알림 문구도 "이 알림을 누르면 바로 업데이트".

## [1.0.62] - 2026-08-08

### Added
- **접속 현황 업데이트 요청 대상 확대**: 버전이 안 뜨는 **구버전(기록 없음)** 가족도 이름 옆에 요청 버튼 표시. **한 번도 설치 안 한 사람(FCM 토큰 없음)은 제외**. (서버 `GET /tokens/members` 추가)

### Changed
- **업데이트 요청 알림 문구 개선**: 보낸이 이름 제거, 실제 아이콘 설명("붉은 원 ⬆ 업데이트 아이콘"), **"주요 개선"은 릴리스 노트(`version.json` notes)에서 자동**으로 채워 매 배포마다 최신 반영(하드코딩 스테일 제거).

## [1.0.59] - 2026-08-08

### Added
- **업데이트 요청 알림**: 관리 기능 → 가족 접속 현황에서 **최신보다 낮은 버전** 가족 이름을 탭 → 확인 후 그 가족에게 **업데이트 요청 FCM 알림** 전송.
- **홈 배경 선택**: 홈 타이틀 **왼쪽 빈 곳 롱클릭** → **기본 코르크 / 우리집 알림판 이미지(`family_board.jpg`)** 선택(기기별 저장, 기본 코르크). 올린 이미지는 EXIF 회전 반영 후 세로 배경용으로 리사이즈해 번들.

## [1.0.55] - 2026-08-08

### Added
- **가족 접속 현황**(관리 기능 → 관리자 전용): 구성원별 **설치 버전 + 마지막 접속 시각**(상대시간). 각자 앱 실행 시 Firestore `presence/{memberId}`에 기록.

### Changed
- **업데이트 확인(종) 트리거 변경**: 30분 주기 폴링 → **'다른 메뉴에서 홈으로 이동할 때'** 확인. 앱을 계속 켜둔 채로도 홈으로 오가면 새 버전이 종으로 뜸(주기 폴링 없음). 일시적 네트워크 실패로 종이 사라지지 않게 보완.

## [1.0.52] - 2026-08-08

### Added
- **요리 레시피 보드**(리스트 카드, 재미진 곳과 동일 형식): 공유 대상 아님 — **재미진 곳/내 재미진 곳에서 '이동'(복사 아님)으로만** 담김. 되돌리기(재미진 곳으로 이동)도 지원.
- **맛집 위젯 위치기반 재검색**: 10분마다(09~23시) 현재 위치 확인 → **시군구가 바뀌면**(또는 1시간 경과·무캐시) 그 지역으로 재검색·갱신. 강제 GPS 없이 lastKnown 사용(배터리 절약).

### Changed
- **맛집 위젯 폰트 표준 고정**(sp→dp): 시스템 글자 크기를 키운 사용자도 위젯 높이가 커지지 않음.
- **재미진 곳/내 재미진 곳/요리 레시피 필터 페이징**: 표시 필터(유튜브/웹/이미지/안 본)를 켜면 **필터 결과 기준으로 페이징**(페이지 수가 실제와 일치, 1페이지뿐인데 여러 페이지로 뜨던 문제 해결). 빈 화면 안내에서 '+로 담아보세요' 제거(수동 추가 없음 반영).

## [1.0.48] - 2026-08-08
D-Day·가족 생일을 달력에 표시.

### Added
- **가족 달력·위젯 달력에 D-Day 항목과 가족 생일 표시**: D-Day 항목(매년 반복이면 해마다)과 4인 생일(🎂, 매년 영구)을 해당 날짜에 표시. **D-Day 추가·수정·삭제 시 앱 달력·위젯 자동 반영**.
  - **표시 전용**(전역 일정에 넣지 않음) → 일정 알림·홈 일정 보드·검색엔 영향 없음. D-Day=전용 주황색, 생일=각자 색. 날짜 탭 시트에선 제외(편집 대상 아님).

## [1.0.45] - 2026-08-08
홈 위젯 3종 추가.

### Added
- **맛집 찾기 위젯**(4×2): 시간당 1회 근처 5km AI 맛집 **20곳을 사진과 함께 캐시** → 화면에서 **10초마다 순차 회전**(네트워크 없음, 09~22시 갱신). 상단 탭=앱 맛집 화면, **카드 탭=네이버플레이스 가게**(앱 AI 추천과 동일), 🔄=다음 곳.
  - 서버 `/recommend`가 구글 플레이스 사진을 함께 반환 + 사진 프록시 `/placephoto`(캐시) 추가, `limit` 파라미터(위젯=20).
- **장바구니 바로가기 위젯**(1×1): 탭=앱 장보기 화면, **살(미체크) 항목 수 배지**.
- **가족 달력 위젯**(4×5): 이번 달 그리드에 **공휴일·가족 일정을 앱 달력과 동일하게 텍스트로**(격자·오늘 강조·요일색, 음력 제외). 탭=앱 가족 달력.

### Fixed
- 위젯 폭주 루프 방지(`onUpdate`에서 fetch 금지 — WorkManager 컴포넌트 토글→PACKAGE_CHANGED→onUpdate 무한루프였음), 회전 고아 알람 가드, 사진 다운로드 IO 디스패처.

## [1.0.31] - 2026-08-08
1.0.26 이후 장보기 바로가기 버튼 모음.

### Added
- **장보기 상단 바로가기 버튼**: 코코달인 앱 실행 버튼 + 쿠팡 장바구니 바로가기 버튼.
  - 쿠팡: 탭 한 번에 설치된 쿠팡 앱의 **장바구니로 직행**(딥링크 `coupang://cart`, 앱 없으면 웹 폴백).
  - 코코달인: 딥링크 없이 **코코달인 앱 실행**(미설치 시 플레이스토어).

### Changed
- 장보기 항목의 **링크 아이콘 제거**(링크 유무는 텍스트 색으로 구분하므로 불필요).

## [1.0.26] - 2026-08-08
1.0 이후 패치 모음.

### Added
- **장보기 쇼핑 공유 담기**: 쿠팡·코코달인(코스트코) 상품을 다른 앱에서 "공유 → 가족 알림판" 하면 상품명으로 **장보기에 자동 추가**(선택 다이얼로그 없음).
  - 쿠팡: 공유 텍스트에서 상품명 추출(프로모 문구 무관, 링크 직전 줄).
  - 코코달인: 이미지+캡션으로 공유됨 → 텍스트의 `product_id`를 서버(`/coco/names`)가 코코달인 API로 조회해 상품명 저장(부분 실패 시 id별 폴백).
  - 장보기 항목에 **출처 배지**(직접/쿠팡/코코달인), 링크 있으면 탭 시 **브라우저로 열기**.
- 맛집·가볼 곳 AI 추천에 **"카테고리, 지역/근처" 직접 입력창**(근처=현재 위치 반경 600m, 평점 4.3 우선·3.5까지·리뷰5+·평점순).

### Changed
- 홈·맛집 목록 스크롤 개선(홈은 fling 부스트, 긴 목록은 기본 감속+contentType), 맛집·가볼곳·재미진곳 수동 추가(+) 버튼 제거(등록은 공유로).
- 홈 오늘 일정 강조, D-Day 제목–날짜 간격, 날씨 오늘/내일 한 행.

### Fixed
- 일정 삭제 확인 다이얼로그(비반복 포함), 여러 날 일정 셀 정렬(막대 끊김), 공유 저장 후 백스택 중복, 문서 열기 한 번만/항상 창.

## [1.0] - 2026-08-07
첫 정식 릴리스. 앱 이름 **가족보드 → 가족 알림판**.

### 홈 화면
- 코르크보드 홈: 가족 공지 포스트잇, 일정 보드, D-Day 카운트다운.
- 일정 보드: 지난/다가오는 일정, 오른쪽에 대상 인원 이름, 날짜 뒤 요일, 좌상단 오늘 날짜/요일.
- 오늘/내일 날씨(고양시 일산동구 백석동 고정, Open-Meteo): 최저(파랑)/최고(빨강) 한 행 + 아이콘, 1시간마다 갱신.
- 오늘 시작 시간이 지난 당일 일정은 자동으로 '지난 일정'으로 이동(여러 날 일정 제외).
- 새 공지 강조 스포트라이트: 새로 체크된 공지가 홈 진입 시 가운데로 떠올라 압정이 꾹 찍히는 효과 → '확인' 시 축소·도킹, 사용자별 확인 상태 저장(viewedBy).
- 표시할(체크된) 공지가 없으면 공지 섹션을 감추고 일정 보드를 위로.

### 보드 · 기능
- **가족 공유 문서함**: pdf·이미지·docx·엑셀 등 업로드 및 다른 앱에서 '공유'로 저장. 열람 대상 지정(기본 모두, 지정 시 해당 멤버·올린이·관리자만), 올린이·관리자(선일) 수정·삭제, 최신순, 제목 기본값은 확장자 제외, 열 때 '한 번만'.
- 맛집·가볼 곳 **AI 추천**: 구글 평점(4.3+)으로 발굴 → 네이버 지역검색으로 실제 상호 검증 → 최대 5곳, 저장/모두저장. 카테고리·지역 필터, 기본 거리순(화면 열기/포그라운드 복귀 시 현재 위치 자동 최신화), 길찾기 앱 선택(T맵·네이버·카카오·구글 + 항상).
- 재미진 곳/내 재미진 곳: 페이지네이션 + 이어보기, 500px 서버 썸네일, 초장문 이미지 타일 뷰어.

### 개선 · 수정
- 공유(재미진 곳·장소·문서함) 저장 후 백스택에 화면이 중복 누적돼 뒤로가기가 여러 번 필요하던 버그 수정.
- 맛집·가볼 곳 거리순에서 위치 갱신 시 목록이 중간에 걸리던 포커스 버그 수정(항상 최상단).
- 준호 재미진 곳 시간제한(2026-11-20) 해제.
- 전 보드 삭제 확인 다이얼로그(리스트·맛집·가볼곳·용돈·D-Day·버킷·공지·재미진 곳).

## 초기 개발 (1.0 이전 누적)
### Added
- 프로젝트 초기 뼈대 (Jetpack Compose + Material 3).
- 온보딩: 가족 4명(선일/은선/준영/준호) 중 본인 선택으로 사용자 식별.
- 캘린더: 월간 보기, 한국 공휴일 표시(공공데이터 API 연동 준비), 구성원 색상 일정, 일정 추가 화면.
- 리스트: 장보기/할 일 공유 체크리스트 (추가/완료/삭제, 작성자 표시).
- 데이터 계층: BoardRepository 추상화 + 인메모리 구현(기본) + Firestore 구현(대기).
- DataStore 기반 본인 멤버 저장.

- Firebase Firestore 실시간 공유 연결 + 익명 인증 기반 보안 규칙.
- 반복 일정 자동 전개(매주/매월/매년 + 음력 매년).
- 일정 미리 알림(시간 기반, AlarmManager) — 폰 알림 발화.
- 캘린더 음력 날짜 + 24절기 표시, 담당자 이름 태그, 복수 담당자.
- 용돈 정산 탭(준영/준호, 금액 합계·천단위 콤마).
- 일정 수정 기능 + 반복 일정 삭제 확인(이 날짜만/모든 반복).
- 일정 상세 내용(최대 300자, URL 클릭 시 외부 브라우저).
- 일정 사진 첨부(갤러리/카메라, 1MB↑ 자동 압축, 최대 5장) — 헤르메스 서버 저장.
- 시작 변경 시 종료 자동 이동, 달력 드래그로 여러 날 일정 추가.
- 등록 알림(FCM): 남이 나를 태깅해 일정/할일 등록 시 내 폰 푸시(등록자 제외) — 헤르메스 서버 릴레이.
- 할 일/장보기 담당자 태깅(복수) + 태그 표시 + 할일 등록 알림.
- 한국 공휴일 표시(공공데이터 특일정보 API, 대체공휴일·음력명절 포함, 실패 시 fallback).

### 대기 (활성화 필요)
- 공휴일 API 서비스키 (local.properties).
- 등록 알림(FCM 푸시), 할 일 담당자 태깅, 반복 발생별 알림 예약.
