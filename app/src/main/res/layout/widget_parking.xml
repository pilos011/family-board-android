<?xml version="1.0" encoding="utf-8"?>
<!-- 1x1 주차 위치. 데이터 모드: 층만 상하로 꽉(위=모닝 베이지 / 아래=그랜저 검정).
     대기 모드: "주차/확인" 두 줄. 탭→조회. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/parking_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@drawable/widget_parking_bg_split"
    android:padding="2dp">

    <!-- 대기: "주차/확인" 두 줄(파란 배경으로 전환됨) -->
    <TextView
        android:id="@+id/parking_click"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:maxLines="2"
        android:lineSpacingMultiplier="0.95"
        android:includeFontPadding="false"
        android:textColor="#FFFFFFFF"
        android:textStyle="bold"
        android:textSize="26sp"
        android:visibility="gone"
        android:text="주차\n확인" />

    <!-- 모닝(첫째 줄): 베이지 -->
    <TextView
        android:id="@+id/parking_morning"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="center"
        android:maxLines="1"
        android:ellipsize="end"
        android:includeFontPadding="false"
        android:textColor="#FFF3E5B3"
        android:textStyle="bold"
        android:textSize="30sp"
        android:text="B2" />

    <!-- 그랜저(둘째 줄): 검정 -->
    <TextView
        android:id="@+id/parking_grandeur"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="center"
        android:maxLines="1"
        android:ellipsize="end"
        android:includeFontPadding="false"
        android:textColor="#FF000000"
        android:textStyle="bold"
        android:textSize="30sp"
        android:text="B4" />
</LinearLayout>
